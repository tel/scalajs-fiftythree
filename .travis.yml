language: scala
sudo: false
scala:
  - 2.11.7
jdk:
  - oraclejdk7
cache:
  directories
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot/
before_cache:
  - find $HOME/.ivy2 -name "ivydata-*.properties" -delete
  - find $HOME/.sbt -name "*.lock" -delete
